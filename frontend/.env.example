# Frontend environment variables (copy to frontend/.env or set in Vercel/Netlify)
# In production the frontend calls the deployed backend directly (no dev proxy).
# Set this to your Render backend URL, e.g. https://nutrimeasure-backend.onrender.com
VITE_API_BASE_URL=
